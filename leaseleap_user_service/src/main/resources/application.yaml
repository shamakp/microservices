server:
  port : 8081

spring:
  security:
    user:
      name: shamakp
      password: shamakp2493
      roles: USER
  jackson:
    deserialization:
      fail-on-unknown-properties: false
    
  datasource:
    url: "jdbc:postgresql://localhost:5432/leaseleap"
    username: postgres
    password: root
    
  jpa:
    show_sql: true
    hibernate:
      ddl-auto: update